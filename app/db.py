# Simple in-memory database for demo purposes
# In production, you would use a real database like PostgreSQL, MongoDB, etc.

users_db = {
    # Example user (username: password_hash)
    # "admin": {"username": "admin", "password": "$2b$12$..."}
}
